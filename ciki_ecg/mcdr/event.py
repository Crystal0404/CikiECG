from mcdreforged import LiteralEvent

PLUGIN_ID = "ciki_ecg"
POWER_OFF = LiteralEvent(f"{PLUGIN_ID}.power_off")
POWER_ON = LiteralEvent(f"{PLUGIN_ID}.power_on")
SERVER_STOP = LiteralEvent(f"{PLUGIN_ID}.server_stop")
